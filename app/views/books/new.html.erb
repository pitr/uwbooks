<h1>Posting Books</h1>

<%= form_for @book do |f| %>
  <%= render :partial => 'books/form', :locals => {:f => f} %>
  <div class="actions">
    <%= f.submit "Post Book", :disable_with => 'Posting...' %>
  </div>
<% end %>

<%= link_to 'Back', books_path %>
