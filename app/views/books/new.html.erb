<h1>Adding Books</h1>

<%= form_for @book do |f| %>
  <%= render :partial => 'books/form', :locals => {:f => f} %>
  <div class="actions">
    <%= f.submit "Add Book", :disable_with => 'Adding...' %>
  </div>
<% end %>

<%= link_to 'Back', books_path %>
